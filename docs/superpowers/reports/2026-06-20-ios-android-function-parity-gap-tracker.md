# iOS Android Function Parity Gap Tracker

日期：2026-06-21
状态：开发中

本文件记录实现过程中发现但尚未关闭的 gap。只有记录了关闭条件的 gap 才允许保留。

| Gap ID | 功能 | 类型 | 当前状态 | 临时处理 | 关闭条件 |
| --- | --- | --- | --- | --- | --- |
| GAP-IOS-APNS-REALDEVICE-001 | 普通 APNs fallback 真机验收 | 测试环境限制 | 当前开发基线使用 iPhone 17 Pro Simulator，模拟器不能验证普通 APNs device token 和通知 fallback 投递。 | 代码必须实现权限/token/status plumbing；模拟器报告只标记为未覆盖，不标记通过。 | 使用真机授权通知，确认 `POST /api/ios/devices/register` 上传非空 `apns_device_token`，并验证 Live Activity update 失败后普通通知 fallback 投递。 |
| GAP-IOS-DYNAMIC-ISLAND-REALDEVICE-002 | Dynamic Island 物理展示 | 测试环境限制 | iPhone 16e 无 Dynamic Island，不能作为灵动岛展示验收设备。2026-06-21 已改用 iPhone 17 Pro Simulator 验证：Settings `Preview` 后 compact 状态显示 `N` 和 unread `1`，点击灵动岛可回到 Nomi Settings；展开态左侧裁切问题已修复，长按展开态 runtime snapshot 为 `N, 1, Nomi, Chat, Nomi is replying, Local Dynamic Island preview`。该结果证明模拟器 ActivityKit UI 和 deep link 可用，但仍不能等价证明真实灵动岛硬件展示。 | 后续模拟器回归必须使用支持 Dynamic Island 的设备型号；真机展示仍作为单独验收项。 | 在支持灵动岛的 iPhone 真机上录屏确认 compact/expanded 状态、点击跳转和未读清除。 |
| GAP-IOS-REALTIME-UI-003 | `/ws` 主动事件自动注入 UI | 已关闭 | `RootView` 已根据服务器配置启动 `NomiRealtimeClient`，`AppState.handleRealtimeEvent` 已把 `proactive_message` 注入 Suggestions store，把 `agent_task_delivery/fallback` 注入 Tasks store，并调用 Live Activity proactive/task 更新。 | 无。 | 已由 `AppStateTests.testRealtimeEventsUpdateSharedStores`、`NomiLiveActivityControllerTests` 和全量 iOS 测试覆盖；仍需在真实后端回归中验证事件投递。 |
| GAP-IOS-CHAT-STREAMING-006 | Chat 使用 `/ws` 流式 delta 驱动 UI | 实现未完成 | Chat 发送仍走 HTTP `POST /api/chat`，已补 `client_context_delta`，但还没有用 `/ws` 的 `chat_delta/chat_done` 更新 pending bubble，也没有 WS 超时后复用同一 pending bubble fallback。 | 文本聊天可用，HTTP fallback 等价于主路径；Live Activity token chunk 只来自 HTTP answer 分块。 | ChatView/NomiChatStore 优先走 `NomiRealtimeClient.sendChat`，收到 `chat_delta` 更新同一 pending bubble，`chat_done` 完成，超时/error 后以同一 `client_request_id` fallback 到 HTTP 且不重复用户消息。 |
| GAP-IOS-TASK-EFFECTS-004 | 长任务 external effect 全操作 | 实现未完成 | Tasks mode 已支持详情/events、resume/cancel，API client 定义了部分任务接口；external effect confirm/execute/rollback/compensation UI 尚未完整呈现。 | 复杂任务可打开 Web 工作台处理；App 内只覆盖基础任务操作。 | Tasks UI 展示 external effect 卡片，支持 confirm/execute/rollback/compensation/confirm 并有成功和失败验收证据。 |
| GAP-IOS-VOICE-STREAMING-005 | 真实麦克风流式 ASR | 实现未完成 | 已补 `voice_start`/`audio_chunk` payload helper、置信度策略和测试；Chat UI 尚未完成 AVAudioEngine 采集、16k mono pcm_s16le 转换、`/ws/voice` 流式发送和 partial/final UI。 | Chat 仍以文本输入为主；语音入口后续必须接真实控制器。 | 真机或模拟器授权麦克风后，能连接 `/ws/voice`，发送 16k mono pcm_s16le audio chunks，显示 partial/final，按置信度自动发送/确认/拒绝。 |
| GAP-IOS-BACKEND-E2E-007 | iOS 主流程真实后端数据验收 | 已关闭 | 2026-06-21 已完成真实服务器 `http://206.119.171.141` / `par-dev` 内容级验收，不接受本机、模拟器 localhost 或临时代理作为通过证据。Chat 已验证固定 token 和算术结果。Suggestions UI 成功加载真实建议，`完成` 写回后 `ad3d151f-8e35-4d32-a471-06ad489a5685` 不再出现在 open 列表，`忽略` 写回后 `196bd80f-756a-4c4a-b08f-4d9e39694acf` 不再出现在 open 列表。Tasks 通过真实创建的 `lta_d1216eda0d694d898d298afb32d74882` 验证 detail/events/resume/cancel，状态最终为 `cancelled` 且事件包含 `task.cancelled`。Accounts 成功加载 3 个 Nomi identities、collector 状态、Composio 连接入口和 remote browser 入口；iOS 已补齐真实后端 envelope 解码和 Composio toolkit 状态合并，Gmail 显示 `healthy` / `Already connected`。Career 成功加载 profile/opportunity/application，并通过 UI `标记已投递` 将 `application_job_pm_001_upsert_stage` 写为 `submitted/submitted/prepare_interview_if_replied`。 | 无。真实验收中发现的内容质量和交互问题已拆成单独 gap。 | 已满足：四个 tab 均已用真实服务器做 UI + 响应正文/关键字段验收。后续回归仍必须使用真实服务器并检查内容，不得只看 HTTP 200。 |
| GAP-IOS-SUGGESTION-CONTENT-008 | Suggestions 历史建议正文污染 | 内容质量缺陷 | 真实服务器 open suggestions 仍有 `fa9f55a1-027b-4d9a-97dd-d2ffab38207c` 正文包含 Python/JSON 字典字符串，如 `{'body': ..., 'subject': ...}`；iOS 能加载和操作，但内容不够产品化。 | iOS 当前如实展示后端内容，不在客户端二次清洗，避免遮蔽数据源污染。 | 后端写入 proactive suggestion 前规范化邮件/事件正文，`GET /api/suggestions?limit=10` 不再返回包含 `{'body':`、`'subject':` 或 HTML escape 的正文；iOS Suggestions 截图中不再出现字典字符串。 |
| GAP-IOS-TASK-READ-SIDE-EFFECT-009 | Tasks 读取产生 checkpoint.restored 副作用 | 后端/交互缺陷 | 验收 `lta_d1216eda0d694d898d298afb32d74882` 时，读取 task detail/events 和 resume 后事件列表出现大量 `checkpoint.restored`；最终 cancel 成功，但事件流被读取/恢复噪声污染。 | iOS 继续显示真实事件；报告中明确该噪声不影响 resume/cancel 写回结果，但不能算事件流质量完全通过。 | `GET /api/agent-tasks/{id}` 和 `/events` 不应因为只读验收追加重复 `checkpoint.restored`；同一任务多次刷新后事件数稳定或只增加由用户操作触发的事件。 |
| GAP-IOS-TASK-STATUS-UI-010 | Tasks action 后状态反馈不够明确 | iOS UI 缺陷 | Resume/Cancel 写回真实服务器成功，但 iOS Tasks 卡片主要显示 `original_goal`，没有清晰显示 `status=current_node` 的最新状态；Cancel 后需要查接口才能确认 `cancelled`。 | 暂以真实接口响应作为验收依据；UI 中仍展示 task id、title 和事件。 | Tasks detail 区同时显示 `status`、`current_node` 或最新动作结果；点击 Resume/Cancel 后 UI 可见状态与 `GET /api/agent-tasks/{id}` 一致。 |
| GAP-IOS-CAREER-CONTENT-011 | Career 存量 opportunity 字段不完整 | 内容质量缺陷 | 真实 Career board 有一条 `job_pm_001` opportunity 的 title/company 为空，iOS 显示为 `· update_fit_score`；这说明页面可加载，但存量数据质量不足。 | iOS 如实展示后端字段；新写入的 `job_ai_agent_product_manager_example_ai` 能显示 `AI Agent Product Manager / Example AI`。 | Career board 的 opportunity 记录必须至少有可读 title 或 fallback 文案；`GET /api/career/board?limit=10` 中不再出现 title/company 同为空导致的 `· status` 卡片。 |
| GAP-IOS-ACCOUNT-WHATSAPP-RELOAD-012 | WhatsApp 云主机登录页首次打开稳定性 | 云主机浏览器稳定性/账号登录验收 | 本地代码已修：`execute_browser_open_command` 对同 host 的显式 `Open browser` 也会重新 `goto` 目标 URL，避免停留在 Chrome `Aw, Snap!`；新增测试先失败后通过，相关 Python 测试 `22 passed`。真实云主机尚未部署，SSH 默认端口要求密码，`10799` 连接被关闭。 | iOS 已提供本地 Reload 按钮；线上部署前如仍遇到崩溃页，用户可点远端或本地刷新。 | 将修复部署到 `/opt/nomi` 并重启 `chromium-runtime` 后，连续多次从 iOS Accounts 点击 WhatsApp `Open browser`，无需手动 Reload 即直接显示 WhatsApp Web QR 或已登录态页面；截图和 collector 状态验证通过。 |
| GAP-IOS-ACCOUNT-LINKEDIN-NAV-013 | LinkedIn 云主机登录页可视切源 | 云主机浏览器命令执行/账号登录验收 | 本地代码已修：runtime 在切源前保留上一手动聚焦来源，目标页不存在时复用用户正在看的可视页导航到 `https://www.linkedin.com/login`；新增测试先失败后通过。iOS 也新增本地来源菜单，可直接切换 WhatsApp/LinkedIn/Telegram/Search/Shopping。真实云主机尚未部署，SSH 默认端口要求密码，`10799` 连接被关闭。 | 不把线上 LinkedIn 可视登录页记为通过；本地模拟器已验证 iOS 顶部来源菜单可见，线上部署前可手动在远端地址栏输入 LinkedIn URL。 | 将修复部署到 `/opt/nomi` 并重启 `chromium-runtime` 后，从 iOS Accounts 或 noVNC 顶部来源菜单点击 LinkedIn，无需手动改地址栏即可显示 `linkedin.com/login` 登录页；截图、URL 和 `/api/browser/open` 返回内容三者一致。 |
