# Long-Tail Agent Runtime V2 Gap Tracker

**Date:** 2026-06-03
**Source spec:** `docs/superpowers/specs/2026-06-03-long-tail-agent-runtime-design-v2.md`
**Rule:** Every implementation pass must update this file. If something is incomplete, blocked, simulated, or only partly verified, it must be written here explicitly.

## Gap Status

| ID | Requirement | Status | Evidence | Remaining Work |
| --- | --- | --- | --- | --- |
| G1 | Long-tail tasks run as explicit graph runs, not free-form agent loops | Implemented | `LongTailGraphRunner` creates validated graph tasks, blocks invalid plans before tool execution, builds step packets, verifies executor results, creates final delivery from verified steps, handles human input, and recovers graph state after local and online process restart. `/api/agent-tasks/route` normalizes OpenClaw-style long-tail work to `route_type=long_tail_agent`. Local regression script and deployed online API validation both return `reasonable: true`. | No open gap for the current V2 runtime. Future model-generated planners must keep producing the same graph schema before execution. |
| G2 | Event log is source of truth and derived state can rebuild from it | Implemented | `PostgresLongTailEventStore` writes/reads `long_tail_task_events` with monotonic sequence and materializes derived tables. Online restart regression created task `lta_1d0a1b61e92d433899ddcfdf6ccea426`, advanced it to `awaiting_executor`, restarted `runtime-api`, then `/resume` recovered `current_node=awaiting_executor` and `current_step_id=inspect_page` from Postgres events. | No open gap. |
| G3 | Every executor action uses `ActionRequest` before execution | Implemented | Browser login open, OpenClaw execution, Composio connect-link creation, and Composio toolkit sync all write `executor.action_requested` before live work. Focused tests assert the event order. Online Postgres check for `composio:readonly:toolkits:sync` shows `executor.action_requested -> policy.checked -> executor.live_completed`. | No open gap for existing executors. Any newly added third-party executor must use `ExecutorAdapterRegistry` before execution. |
| G4 | PolicyGate blocks or pauses risky actions before adapter execution | Implemented | `PolicyGate` blocks `browser.submit`, blocks actions outside allowlists, requires confirmation for message/send/payment-style risks, and is now enforced in OpenClaw, managed browser, and Composio live paths. Validation script proves blocked live submit never invokes the adapter callback. | No open gap. |
| G5 | Verifier requires independent evidence, not executor summary only | Implemented | `StepVerifier` rejects missing evidence, rejects forbidden external action events, checks step-type evidence requirements, passes browser/DOM evidence, and emits fallback action cards when verifier blocks a step. Validation script includes `verification_passed` and `fallback_external_action_card` semantic checks. | No open gap for current step types. Future step types should add evidence requirements before being enabled. |
| G6 | External effects require confirmation event before execution | Implemented | `ExternalEffectController` keeps proposed effects in `waiting_for_confirmation`, refuses execution before confirmation, recovers effect state from event log, writes `external_effect.confirmed` before `external_effect.executed`, and API endpoints use this controller. Validation script and API tests prove premature execution returns `may_execute=false`. Existing live adapters only perform read/draft/queue operations and do not bypass this controller for send/pay/submit. | No current uncontrolled external-effect path found. Real future Composio tool execution for send/pay/submit must be added as an external-effect proposal/confirmation/execute flow, not direct execution. |
| G7 | Rollback only restores Nomi internal state and never claims third-party rollback | Implemented | API rollback returns `external_world_can_rollback=false` and a compensation action card. H5 workbench renders delivery/fallback cards and opens rollback/compensation flows. Android Realtime can pass `agent_task_delivery` and `agent_task_fallback` events into H5 through `nomi-pending-agent-event`. Validation script checks all UI bridge strings/styles. | No open gap. |
| G8 | Durable task leases, restart recovery, and checkpoints | Implemented | Local lease/recovery tests pass; `PostgresLongTailLeaseManager`, `PostgresLongTailRecoveryScanner`, and background recovery loop are registered outside test DB mode. Online restart regression after a real `runtime-api` restart recovered the pending executor step from deployed Postgres. | No open gap. |
| G9 | OpenClaw/Composio/browser adapters are step executors only | Implemented | `ExecutorAdapterRegistry` records action request, policy report, trace ids, dry-run completion, and live completion. OpenClaw live/dry-run, managed browser command queue, Composio authorization, and Composio toolkit sync all run through this boundary. Online Composio sync returned 10 toolkits with Gmail connected and no secret leakage; Postgres event order was verified. | No open gap for implemented adapters. Full Composio meta-tool execution is not enabled yet; when added, it must reuse this same step-executor boundary. |
| G10 | API exposes task state, events, resume, cancel, confirm, human input | Implemented | Password-protected `/api/agent-tasks` route/create/state/events/run-next/complete-step/finalize/cancel/resume/human-input/confirm and external-effect endpoints exist and are covered locally. Online deployed API validation returned 200 for route/create/state/events and `reasonable=true`; online process restart recovery returned `Task is waiting for executor result.` | No open gap. |
| G11 | Regression script inspects intermediate outputs for semantic reasonableness | Implemented | `scripts/validate-long-tail-agent-runtime-v2.py` now prints 21 stages including online deployed API mode. Local run: 21 stages, no unreasonable outputs, online stage explicitly `skipped` without env. Online run with `NOMI_ONLINE_BASE_URL=http://206.119.171.141` and password env: final stage `online_deployed_api_mode` returned `status=validated`, route/create/state/events 200, `route_type=long_tail_agent`, event count >= 6. | No open gap. |

## Update Log

- 2026-06-03: Gap tracker created before implementation starts. All V2 runtime items are currently open.
- 2026-06-03: Implemented first local primitives for G2-G5 with focused tests. These are not yet wired to API, Postgres bootstrap, or live adapters.
- 2026-06-03: Added first `LongTailGraphRunner` skeleton for G1. It validates plans, writes graph events, and builds step packets, but does not yet run live executors or final evaluation.
- 2026-06-03: Added external-effect confirmation controller for G6-G7. It blocks execution before confirmation and only offers compensation after external effects.
- 2026-06-03: Added local executor adapter boundary for G9. It records `ActionRequest` and policy decisions before execution, but live adapters are not yet wired.
- 2026-06-03: Wired `StepVerifier` into graph runner completion. Passing steps write memory patch + checkpoint; failed verification writes fallback without checkpoint.
- 2026-06-03: Added final evaluator/delivery behavior. Completed verified steps produce `final.evaluated`, `delivery.created`, and `task.completed`; missing steps return partial delivery.
- 2026-06-03: Added first `/api/agent-tasks` endpoint set for G10. Current API uses the in-memory graph runner and is not yet backed by persistent event-store operations.
- 2026-06-03: Added V2 regression script for G11. It checks intermediate semantic artifacts locally; online/live adapter mode is still pending.
- 2026-06-03: Added local restart recovery and V2 lease manager for G8. Recovery now rebuilds graph state from events and the regression script checks a `restart_recovery` stage. Persistent Postgres leases and online recovery scanner remain incomplete.
- 2026-06-03: Expanded G10 API coverage. `/api/agent-tasks/{task_id}/resume` now recovers missing runner state from the event log; human input and confirmation events are verified through the events API. Persistent Postgres-backed API remains incomplete.
- 2026-06-03: Closed the local child-trace part of G9. Adapter action proposals can now carry provider/executor trace ids into long-tail events; live adapter execution still needs to be routed through this boundary.
- 2026-06-03: Added `PostgresLongTailEventStore` and wired `main.py` to use it outside `postgresql://test`. This makes the append-only event log durable in real environments, but materialized V2 tables and online restart regression are still pending.
- 2026-06-03: Added materialization for core Postgres events: task runs, plans, step runs, action requests, policy reports, and checkpoints.
- 2026-06-03: Added materialization for verifier results, memory patches, human input, and external effects. Leases and online recovery scanning remain incomplete.
- 2026-06-03: Added Postgres lease manager and recovery scanner primitives.
- 2026-06-03: Wired the long-tail recovery scanner into a background loop. Unit tests verify claim/recover/release behavior; online process-restart observation is still pending.
- 2026-06-03: Added API route normalization test and top-level `capability_id` for `/api/agent-tasks/route`, so OpenClaw-style long-tail requests are explicitly routed through `long_tail_agent`.
- 2026-06-03: Added durable pending-human-input request/resolve semantics. `human_input.requested` now stores the question/options/status in runner state, materializes to Postgres, and recovers from the event log; API state exposes the same payload until `/human-input` resolves it.
- 2026-06-03: Extended the V2 validation script with `human_input_waiting` and `human_input_resolved` stages. The script now checks the human-input payload semantically and verifies requested/received events are present in the trace.
- 2026-06-03: Extended the V2 validation script with external-effect confirmation stages. It now demonstrates that external effects cannot execute before confirmation and that confirmed execution writes `external_effect.confirmed` and `external_effect.executed` into the trace.
- 2026-06-03: Extended the V2 validation script with local API mode coverage. It uses password-protected FastAPI endpoints to route a browser long-tail request, create a graph task, read task state, and read events.
- 2026-06-03: Added adapter dry-run execution to `ExecutorAdapterRegistry` and the V2 validation script. Dry-run records provider/executor trace ids and `executor.dry_run_completed` while explicitly reporting `external_side_effect=false`.
- 2026-06-03: Wired external-effect propose/confirm/execute API endpoints through `ExternalEffectController`. The controller can recover effect state from the task event log after process-local memory is lost.
- 2026-06-03: Added rollback action-card and compensation API coverage for external effects. Rollback messaging explicitly says third-party actions cannot be undone and offers only a confirmed compensation path.
- 2026-06-03: Wired Android Realtime delivery/fallback events into H5 through `nomi-pending-agent-event`, added H5 action-card surface checks, and added Android unit tests for `agent_task_delivery` / `agent_task_fallback` parsing.
- 2026-06-03: Routed Composio authorization-link creation and toolkit sync through `ExecutorAdapterRegistry.execute_live`, so both now write `executor.action_requested`, `policy.checked`, and `executor.live_completed` before/after SDK calls.
- 2026-06-03: Extended the V2 validation script with an `online_deployed_api_mode` stage. Local default run reports the online stage as explicitly skipped; online configured run validates route/create/state/events against the deployed server.
- 2026-06-03: Deployed the updated code to `/opt/par`, rebuilt/restarted `runtime-api`, `worker`, `chromium-runtime`, and `nginx`, then validated the deployed long-tail API and Composio toolkit sync.
- 2026-06-03: Completed online Postgres restart recovery validation. A task in `awaiting_executor` survived a real `runtime-api` restart and `/resume` recovered `current_step_id=inspect_page` from the event log.
